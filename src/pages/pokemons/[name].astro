---
interface Props{
       url: string
}
import type { GetStaticPaths } from "astro";
import MainLayout from "../../layouts/MainLayout.astro";
import PokemonCard from '../../components/pokemos/PokemonCard.astro';
export const getStaticPaths = (() => {
      return [
        {
            params:{name: 'bulbasaur'},
            props:{name:'bulbasaur'}, url: "https://pokeapi.co/api/v2/pokemon/1/" },
        { 
            params:{name: 'charmander'} ,
            props:{name:'charmander'},url: "https://pokeapi.co/api/v2/pokemon/4/ "}
       
    ];
}) satisfies GetStaticPaths;

const {name} = Astro.params;
const {url} = Astro.props;
 console.log({url});




---

<MainLayout title="Algun pokemon">
<section class="mt-10 mx-10 flex flex-col justify-center items-center">
<div>
    <h1 class="text-5xl capitalize font-bold mb-4">{name}</h1>
    <a href="/">Regresar</a>
</div>

<PokemonCard  name={name} url=""/>
</section>
</MainLayout>


<style>
    a {
        @apply hover:bg-slate-600 bg-slate-800 px-2 py-2 
    }
</style>